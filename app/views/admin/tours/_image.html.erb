<div class="position-relative m-2" id="image_<%= image.id %>">
    <%= image_tag(image, size: Settings.img_tag_size, class: "img-thumbnail") %>
    <%= link_to "x", remove_image_admin_tour_path(id: @tour.id, image_id: image.id),
                data: { turbo_method: :delete },
                class: "position-absolute top-0 end-0 text-danger fw-bold px-1" %>
  </div>
